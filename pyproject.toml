[project]
name = "budget-variance-explainer"
version = "0.1.0"
description = "OpenEnv Budget Variance Analysis"
requires-python = ">=3.9"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic"
]

[tool.setuptools]
py-modules = []